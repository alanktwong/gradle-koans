/*
 * INSTRUCTION:
 * ===========
 * 1. Disable task B so that it will be skipped during execution
 */

ext.list = []

task A {
    println('doing A')
}
task B(dependsOn: 'A') {
    println('doing B')
}
task C(dependsOn: 'B') {
    println('doing C')
}

tasks.each { ext.list.add(it.name) }

// ------------ START EDITING HERE ----------------------
project.B.enabled = false
// ------------ STOP EDITING HERE  ----------------------

task verify(dependsOn: 'C') {
    assert tasks.findByPath('B')
    assert !ext.list.contains('B')
}
